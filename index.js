/* Lógica de Control Modular - LECCIÓN 2 MATEMÁTICAS */

// Banco completo de 50 preguntas estructurado semánticamente
const questions = [
    // El mismo arreglo unificado que está incrustado en el index.html principal para preservar coherencia
    {
        categoria: "Intervalos",
        pregunta: "Si A = (-2, 5] y B = [3, 8), ¿cuál es el resultado de la intersección A ∩ B?",
        opciones: { A: "[3, 5]", B: "(3, 5]", C: "[-2, 8)", D: "(3, 5)" },
        correcta: "A",
        explicacion: "La intersección incluye los números que pertenecen a ambos intervalos. El 3 está en ambos, y el 5 también está incluido en ambos."
    }
    // NOTA: Para el despliegue modular extendido en GitHub, el desarrollador puede enlazar este archivo.
];

// Inserte aquí el resto de las funciones idénticas a las del script inline para control de estado
